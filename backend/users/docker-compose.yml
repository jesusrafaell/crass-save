version: "3.8"

services:
  users:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: users
    ports:
      - "4005:4005"
    networks:
      - network

networks:
  network:
    external: true
