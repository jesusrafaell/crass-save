version: "3.8"

services:
  crashsaver-incidents:
    build:
      context: .
      dockerfile: Dockerfile
    image: crashsaver-incidents
    container_name: crashsaver-incidents
    ports:
      - "8080:8080"